# Copy to backend/.env and fill with your values. Do NOT commit real secrets.
PORT=3000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/schedule-app-database?retryWrites=true&w=majority
JWT_SECRET=<strong-random-64+chars>
# For local dev:
# CORS_ALLOWED_ORIGINS=http://localhost:9000
# For production:
# CORS_ALLOWED_ORIGINS=https://yourdomain.example
